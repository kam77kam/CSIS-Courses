package a01;

/**
 * Coding challenges for assignment A01 part1.
 * 
 * @author CSIS + ....... (add your name here)
 *
 */
public class A01Version2 {
	
	/**
	 * Returns a new array that includes commas as thousands separators.
	 * E.g., ['7', '6', '5', '4', '3', '2', '1'] returns ['7', ',', 6, 5, 4, ',', 3, 2, 1] 
	 * E.g., ['5', '3', '6'] returns ['5', '3', '6']
	 * <p>
	 * If the array is empty, an empty array should be returned. 
	 * 
	 * @param digits original array of digits
	 * @return new array that includes commas as thousands separators.
	 * @throws IllegalArgumentException if the array <code>digits</code> includes
	 * any characters that are not digits.
	 * @throws NullPointerException if the array <code>digits</code> is null.
	 */
	public static char[] addSeparators (char[] digits) {
	    return null; // TODO
	}
	
	
	/**
	 * Finds the shortest sequence of consecutive array elements that add up to 
	 * the specified sum and returns the length of that sequence. 
	 * If no such sequence is found, the method returns 0.    
	 * <p>
	 * E.g., [2, 10, 4, 8, 1, 22, 5, 16, 2] and sum = 23 returns 2
	 * This example has three sequences that add up to 23: 10_4_8_1, 1_22, and 5_16_2.
	 * Since 1_22 is the shortest sequence, the method returns 2.
	 * E.g., [1, 7, 4, 0] and sum = 5 returns 0 because no sequence 
	 * of consecutive array elements adds up to 5.  
	 * 
	 * @param array
	 * @param sum 
	 * @return the minimum number of consecutive addends to calculate the specified sum. 
	 * @throws IllegalArgumentException if the array is empty or it includes negative numbers.
	 * @throws IllegalArgumentException if the user passes a negative sum as the second argument
	 * @throws NullPointerException if the user passes null as the first argument
	 */
	public static int minNumberOfAddends(int[] array, int sum) {
	    return 0; // TODO
	}

	/**
	 * Changes the two-dimensional array by multiplying each element with 2.
	 * E.g., [3, 4, 1]                 [6, 8, 2]
	 *       [4, 0, 2, -2] changes to  [8, 0, 4, -4]
	 *       [1, 4]                    [2, 8]
	 * <p>
	 * If the two-dimensional array is empty, no changes are made.
	 * 	 
	 * @param array2d
	 * @throws NullPointerException if the argument is null.
	 */
	public static void doubleValues(int[][] array2d) {
		// TODO
	}
	
	/**
	 * Finds the maximum sum between the two diagonals of a square matrix.
	 * It returns the sum of the main diagonal (elements from top-left to bottom-right)
	 * or the sum of the secondary diagonal (elements from bottom-left to top-right), 
	 * whichever is larger.
	 *
	 * @param matrix The square matrix.
	 * @return The maximum sum between the two diagonals.
	 * @throws IllegalArgumentException if the matrix is not square or if it is empty.
	 */
	public static int findMaximumDiagonalSum(int[][] matrix) {
	    return 0; // TODO
	}

} 
